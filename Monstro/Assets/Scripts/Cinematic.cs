using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*

public class Cinematic : AnimatorAnimation
{
    private Player player;
    // Start is called before the first frame update
    void Start()
    {
        GameObject playerObject = GameObject.Find("Player");
        if(playerObject==null)
            Debug.LogError("Cadê o player?");
        else{
            player = playerObject.GetComponent<Player>();
        }
    }

    void DisableMovement(){
        player.upMovementLocked = true;
		player.downMovementLocked = true;
		player.leftMovementLocked = true;
		player.rightMovementLocked = true;
        player.jumpingLocked = true;
    }
    void EnableMovement(){
        player.upMovementLocked = false;
		player.downMovementLocked = false;
		player.leftMovementLocked = false;
		player.rightMovementLocked = false;
        player.jumpingLocked = false;
    }
    void EndCinematic(){
        GetComponent<Animator>().SetTrigger("endTrigger");
    }
}
*/